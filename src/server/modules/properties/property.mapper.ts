import type { PropertyDocument, PublicProperty } from './property.types';
export function mapPublicProperty(document: PropertyDocument): PublicProperty { if (!document._id) throw new Error('Property identifier is missing.'); return { id: document._id.toHexString(), title: document.title, slug: document.slug, propertyType: document.propertyType, location: document.location, price: document.price, lotArea: document.lotArea, floorArea: document.floorArea, shortDescription: document.shortDescription, features: document.features, images: document.images, isFeatured: document.isFeatured, publishedAt: document.publishedAt?.toISOString() ?? null }; }
