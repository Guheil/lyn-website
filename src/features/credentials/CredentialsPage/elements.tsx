'use client';
import { styled } from '@mui/material';
export const StyledSection = styled('section')({ padding: 'clamp(72px,8vw,124px) clamp(20px,4vw,48px)' });
export const StyledInner = styled('div')({ maxWidth: 1440, margin: '0 auto' });
export const StyledWhy = styled('div')(({ theme }) => ({ display: 'grid', gridTemplateColumns: '.9fr 1.1fr', gap: 'clamp(42px,7vw,100px)', '& .list': { display: 'grid', gap: 12 }, '& .item': { padding: 24, border: `1px solid ${theme.palette.divider}`, background: theme.palette.background.paper }, [theme.breakpoints.down('lg')]: { gridTemplateColumns: '1fr' } }));
export const StyledRecords = styled('div')(({ theme }) => ({ display: 'grid', gridTemplateColumns: 'repeat(3,1fr)', gap: 16, marginTop: 44, '& article': { padding: 28, border: `1px solid ${theme.palette.divider}`, background: '#fff', minHeight: 270 }, '& article:first-of-type': { gridColumn: 'span 2' }, '& .icon': { width: 82, height: 82, display: 'grid', placeItems: 'center', border: `1px solid ${theme.palette.primary.main}`, color: theme.palette.primary.dark, fontWeight: 800, marginBottom: 24 }, [theme.breakpoints.down('md')]: { gridTemplateColumns: '1fr', '& article:first-of-type': { gridColumn: 'span 1' } } }));
