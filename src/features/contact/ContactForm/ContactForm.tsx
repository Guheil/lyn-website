'use client';import{useState}from'react';import{Alert,Button,MenuItem,TextField}from'@mui/material';import type{ContactFormProps,ContactFormValues}from'./interface';import{StyledForm}from'./elements';
const initial=(props:ContactFormProps):ContactFormValues=>({name:'',phone:'',email:'',inquiryType:'General inquiry',preferredPropertyType:'Not sure',propertyReference:props.initialReference||'',propertyId:props.propertyId||'',propertySlug:props.propertySlug||'',propertyName:props.propertyName||props.initialReference||'',assignedBrokerId:props.assignedBrokerId||'',assignedTo:props.assignedTo||'',message:'',website:''});
export function ContactForm(props:ContactFormProps){const[values,setValues]=useState(()=>initial(props));const[state,setState]=useState({loading:false,message:'',error:false});const change=(field:keyof ContactFormValues)=>(event:React.ChangeEvent<HTMLInputElement>)=>setValues(v=>({...v,[field]:event.target.value}));async function submit(event:React.FormEvent){event.preventDefault();setState({loading:true,message:'',error:false});try{const response=await fetch('/api/inquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});const result=await response.json()as{message?:string};if(!response.ok)throw new Error(result.message||'The inquiry could not be sent.');setValues(initial(props));setState({loading:false,message:'Your inquiry has been sent to the appropriate property contact.',error:false});}catch(error){setState({loading:false,message:error instanceof Error?error.message:'The inquiry could not be sent.',error:true});}}return<StyledForm onSubmit={submit} noValidate><TextField label="Name" required value={values.name} onChange={change('name')}/><TextField label="Contact number" required value={values.phone} onChange={change('phone')}/><TextField label="Email" type="email" required value={values.email} onChange={change('email')}/><TextField select label="Inquiry type" value={values.inquiryType} onChange={change('inquiryType')}>{['Buying','Selling','Listing consultation','Property marketing','General inquiry'].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField select label="Preferred property type" value={values.preferredPropertyType} onChange={change('preferredPropertyType')}>{['Land','House and Lot','Residential','Commercial','Investment Property','Not sure'].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField label="Location or property reference" value={values.propertyReference} onChange={change('propertyReference')}/><TextField className="full" label="Message" multiline minRows={5} required value={values.message} onChange={change('message')}/><input tabIndex={-1} autoComplete="off" aria-hidden="true" value={values.website} onChange={e=>setValues(v=>({...v,website:e.target.value}))} style={{position:'absolute',left:'-10000px',width:1,height:1}}/>{state.message&&<Alert className="full" severity={state.error?'error':'success'}>{state.message}</Alert>}<Button className="full" type="submit" variant="contained" color="secondary" disabled={state.loading}>{state.loading?'Sending…':'Send inquiry'}</Button></StyledForm>}
