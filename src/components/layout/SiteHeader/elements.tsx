'use client';
import { Box, Button, IconButton, styled } from '@mui/material';
export const StyledHeader = styled('header')(({ theme }) => ({ position: 'sticky', top: 0, zIndex: 50, background: 'rgba(251,250,247,.94)', backdropFilter: 'blur(16px)', borderBottom: `1px solid ${theme.palette.divider}` }));
export const StyledInner = styled('div')(() => ({ maxWidth: 1440, minHeight: 78, margin: '0 auto', padding: '0 clamp(20px,4vw,48px)', display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 24 }));
export const StyledBrand = styled('a')({ display: 'inline-flex', alignItems: 'center', gap: 12, flexShrink: 0 });
export const StyledBrandMark = styled('span')(({ theme }) => ({ width: 42, height: 42, border: `1px solid ${theme.palette.primary.main}`, display: 'grid', placeItems: 'center', color: theme.palette.primary.dark, fontWeight: 800 }));
export const StyledBrandText = styled('span')(({ theme }) => ({ display: 'grid', lineHeight: 1.15, '& strong': { fontSize: '1rem' }, '& small': { color: theme.palette.text.secondary, fontSize: '.76rem' }, [theme.breakpoints.down('sm')]: { '& small': { display: 'none' } } }));
export const StyledNav = styled('nav')(({ theme }) => ({ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 'clamp(18px,2vw,34px)', color: theme.palette.text.secondary, fontSize: '.92rem', flex: 1, '& a': { position: 'relative', paddingBlock: 28, transition: 'color 180ms ease', '&:hover': { color: theme.palette.text.primary }, '&[data-active="true"]': { color: theme.palette.text.primary, fontWeight: 700 }, '&[data-active="true"]::after': { content: '""', position: 'absolute', left: 0, right: 0, bottom: 20, height: 1, background: theme.palette.primary.main } }, [theme.breakpoints.down('md')]: { display: 'none' } }));
export const StyledHeaderAction = styled(Button)(({ theme }) => ({ flexShrink: 0, [theme.breakpoints.down('md')]: { display: 'none' } }));
export const StyledMenuButton = styled(IconButton)(({ theme }) => ({ display: 'none', border: `1px solid ${theme.palette.divider}`, borderRadius: 0, [theme.breakpoints.down('md')]: { display: 'inline-flex' } }));
export const StyledOverlay = styled(Box)({ position: 'fixed', inset: 0, zIndex: 60, background: 'rgba(17,17,15,.36)', backdropFilter: 'blur(4px)' });
export const StyledMobilePanel = styled(Box)(({ theme }) => ({ position: 'fixed', zIndex: 70, top: 0, right: 0, bottom: 0, width: 'min(88vw,380px)', background: theme.palette.background.paper, padding: 24, display: 'grid', alignContent: 'start', gap: 10, boxShadow: '-24px 0 70px rgba(18,18,16,.18)', animation: 'slideIn 220ms ease both', '@keyframes slideIn': { from: { transform: 'translateX(100%)' }, to: { transform: 'translateX(0)' } }, '& a': { minHeight: 54, display: 'flex', alignItems: 'center', paddingInline: 16, borderBottom: `1px solid ${theme.palette.divider}`, fontWeight: 700 }, '& a[data-active="true"]': { color: theme.palette.primary.dark, background: '#f5f1e9' } }));
